export const leftMenuList = state => {
  return state.leftMenuList
}

export const headMenuList = state => {
  return state.headMenuList
}

export const isCollapse = state => {
  return state.isCollapse
}

export const userInfo = state => {
  return state.userInfo
}
